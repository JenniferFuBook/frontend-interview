// TC39 "Type Annotations" proposal (Stage 1): type syntax in plain .js files,
// treated as comments by the engine. NOT executable in any current runtime —
// kept for reference only.

export function add(a: number, b: number): number {
  return a + b;
}
